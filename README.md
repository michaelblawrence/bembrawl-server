# Bembrawl server

```sh
# Start the application in watch dev mode
yarn start

# Transpile the TypeScript files
yarn build

# Webpack build the TypeScript files
yarn build:prod

# Lint the project files using TSLint
yarn lint
```
